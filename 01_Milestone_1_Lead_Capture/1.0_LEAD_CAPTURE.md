# Milestone 1 — Lead Capture

## Objective

Build the first entry point of the Voryn AI Lead-to-Appointment Automation system.

The workflow receives lead information through an HTTP POST request and returns a successful JSON response.

---

## Workflow

```text
Lead Form
    ↓
HTTP POST
    ↓
n8n Webhook
    ↓
Respond to Webhook
    ↓
Success Response